This example was copied from the Excalidraw repository:

- Upstream repository: `https://github.com/excalidraw/excalidraw`
- Upstream commit: `a83ac488536dbf4bc4dcf1f472f72ce3b4bd2073`
- License: MIT

It is included here as a large real-world React **application** (not a component
library) for stress testing and accuracy-oracle runs: route/lazy code-splitting,
dynamic imports, app→feature→shared layering, and workspace packages. It
complements the Chakra UI snapshot, which is library-shaped (barrels and
re-exports).
